# 🛠 TSE Bootcamp Tools

A collection of automation scripts for managing the PocketBase training environment.

## 🚀 Data Seeder (`seed_data.sh`)
This script populates a PocketBase `users` (Auth) collection with 2,000 randomized records, including specific data sets for the "October 1997" cleanup task.

### Usage:
1. Ensure the script is executable:
   ```bash
   chmod +x seed_data.sh
